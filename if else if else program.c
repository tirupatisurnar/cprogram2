#include<stdio.h>
int main () {
    int time = 20;
    if(time < 18) {
        printf("goodbye");
    }else if (time < 20) {
        printf("good morning");
    }else() {
        printf("good evenning");
    }
    return 0;
}